## HEARTBEAT_OK 契约

- **如果没有异常，只回复 `HEARTBEAT_OK`，不要输出任何检查过程。**

只有发现以下情况时，才需要输出内容或使用工具：
- 有超过 3 天未推进的关键事项
- 有待确认决策仍未闭环
- 有“已讨论但未记录”的产研结论
- `memory/` 索引和日志明显过时

## 优先检查

- `USER.md` 里的“当下关心的事”是否仍然准确
- `MEMORY.md` 里的待跟进项是否有应该提醒用户的内容
- 最近是否应该提醒用户确认国际化需求、增长看板、埋点补齐等事项

**一切正常 → 只回复 `HEARTBEAT_OK`。**
